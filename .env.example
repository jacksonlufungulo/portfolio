# Password for the /admin content manager.
# Copy this file to ".env.local" and change the value before deploying.
# If unset, it falls back to "admin" (fine for local dev only).
ADMIN_PASSWORD=admin
